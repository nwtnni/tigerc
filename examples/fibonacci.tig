let
    function fibonacci(n: int): int =
        if n = 0 then
            0
        else if n = 1 then
            1
        else
            fibonacci(n - 1) + fibonacci(n - 2)
in
    prints("fibonacci(40) is ");
    printi(fibonacci(40));
    prints("\n(Should be 102334155)\n")
end
